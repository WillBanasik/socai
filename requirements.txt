# Core
requests>=2.31.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
markdown>=3.5.0
lxml>=4.9.0

# DNS enumeration (exposure tests, SPF/DMARC analysis)
dnspython>=2.6.0

# Optional – headless browser (recommended)
playwright>=1.43.0

# PowerPoint report generation
python-pptx>=0.6.21

# File-format analysers (hard requirements)
pefile>=2023.2.7                # PE/DOS executables (.exe/.dll/.sys)
oletools>=0.60.1                # Office macros (DOC/XLS/DOCM/XLSM), MSI streams
olefile>=0.47                   # OLE2 compound documents (MSI, legacy Office)
pdfminer.six>=20231228          # PDF structure + active content
pikepdf>=8.7.1                  # PDF object/JavaScript extraction
LnkParse3>=1.4.0                # Windows .lnk shortcuts
macholib>=1.16.3                # Mach-O (macOS) binaries
pycdlib>=1.14.0                 # ISO 9660 / Joliet / Rock Ridge / UDF
volatility3>=2.7.0              # Memory dump analysis (pslist/netscan/malfind/etc.)

# Optional – YARA scanning
# yara-python>=4.3.0

# Optional – native EVTX parsing
# python-evtx>=0.7.0

# Optional – local GeoIP lookups (requires MAXMIND_LICENSE_KEY in .env)
# geoip2>=4.8.0

# Optional – infostealer log parsing (dark web tools)
# stealer-parser>=0.3.0

# MCP server
mcp>=1.1.0
# Pin starlette / sse-starlette to a range compatible with both mcp and any
# FastAPI 0.x ecosystem packages that may share the environment. Without
# these, pip will resolve sse-starlette to 3.x (requires starlette>=0.49.1)
# which breaks FastAPI <0.135.
starlette>=0.40,<0.48
sse-starlette>=1.6.1,<3

# Auth (shared: MCP server + user management)
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# Dev / testing
pytest>=8.0.0
